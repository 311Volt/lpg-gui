TODO:
	- padding for gui::Text
	- fix window edges not fitting in the clipping rectangle
	- gui::ProgressBar
	- gui::DropdownList
	- gui::Radio
	- gui theme .ini files: make paths relative to the file somehow
	- replace event handler system with al::EventLoop
	- Window::getScreenSize -> Window::getSizeOnScreen
	- add ctor for Text that defaults to size=(ScreenW,ScreenH) for auto size mode
	- change Point to Vec2 where appropriate
	- Window::addChildren
	- pick a consistent order of x,y,w,h in window ctors
	- add a version of Window::give that takes a raw pointer
	- check everything for const correctness
	- the name onMouseMove() in some gui classes might be misleading (bc of reg/del in callbacks)
	- textures for windows
	- overflow=ellipses/hidden/scroll for gui::Text
	- gui::Env
	- ResourceManager: fallback support (in particular, make the library not need file i/o to work)
	- gui::ColorPicker
	- gui::NumberInput
	- move #includes from headers to sources where possible
	- make ResourceManager work in its own thread
	- figure out some sane way of initializing the resource manager
	- replace draw calls in Window::render() by outputting vertex buffers like ImGui
	- move all rendering to a separate backend?
	- parallelize rendering and gui logic
	- add multithreaded mode

DONE:
	- multiline support for gui::Text
	- make resource objects guaranteed to be valid and introduce handle objects 
		(done with axxegro and new RM)
	- formatting tokens for Text (as in Minecraft or TMNF) 
	- port everything to axxegro
	- gui::TextBox
	- ResMgr: add u32 handles
	- IntegerMap: reduce code redundancy
	- IntegerMap [to replace unordered_map<uint32_t, T>]
	- event dispatch system (registerEventHandler)
	- fix that fucking segfault
	- gui::Window::give
	- ResourceManager
		- parsing sections: [Bitmap], [Sample], [Font]
		- ResourceManager::give(Resource&& res, std::string name)
		- garbage collection
	- al::Font, lpg::VectorImage: read parameters from source string
	- lpg::VectorImage
	- al::Resource::Resource(Resource&& other)
	- pre-rendered windows
	- name change: gui::Static -> gui::Text
	- add al::Log()
	- al::Config: unordered_map interface
	- gui::Button as base class
	- gui::Checkbox as Button subclass
	- refactor: add() to addChild()
	- gui::Checkbox
	- gui::Image
	- gui::Slider